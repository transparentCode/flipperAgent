version: "1"

# Baseline placeholders—not optimized values.
defaults:
  detection:
    pivot_span_bars: 5
    zone_half_width_atr: 0.25

  association:
    merge_distance_atr: 0.50

  lifecycle:
    touch_tolerance_atr: 0.25
    break_buffer_atr: 0.25
    break_confirm_closes: 2
    max_age_bars: 50

  runtime:
    max_active_zones: 8

timeframes: {}

assets: {}
