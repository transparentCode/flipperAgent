"""Model template — copy this directory when creating a new model.

Rename the directory and update all placeholder values.
"""
# This template is intentionally excluded from auto-discovery
# because the directory name starts with an underscore.
