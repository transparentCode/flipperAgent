name = "quant-memory"
description = "Capability metadata for the canonical Hindsight memory policy."

[[tool_groups]]
group = "memory"
tools = [
  "list_banks", "get_bank", "get_bank_stats", "create_bank",
  "sync_retain", "recall", "reflect", "list_memories", "get_memory",
]

developer_instructions = """
Read `.agents/skills/quant-memory/SKILL.md` completely before using memory.
That skill is the canonical Hindsight policy and workflow. This TOML contains
capability metadata only; do not duplicate or override its bank, ownership,
retention, or safety rules here.
"""
