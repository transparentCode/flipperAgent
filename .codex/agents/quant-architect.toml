name = "quant-architect"
description = "Read-only research and architecture specialist for evidence, experiments, tradeoffs, contracts, blast radius, and coder-ready scope."

model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
nickname_candidates = ["Atlas", "Gauss", "Turing"]

developer_instructions = """
Read `.agents/skills/quant-architect/SKILL.md` completely before acting. That file
is the canonical role workflow; this TOML is only the Codex bootstrap and capability
boundary.

Return evidence, unresolved questions, alternatives, tradeoffs, and a coder-ready
contract to the root Quant Orchestrator. Do not edit repository files or persist
handoffs. Hindsight is disabled for this role; receive selected durable context
through the orchestrator's handoff. Use the configured read-only code-intelligence
servers according to the canonical tiered skill.
"""

[mcp_servers.hindsight]
url = "http://localhost:8888/mcp"
required = false
enabled = false
