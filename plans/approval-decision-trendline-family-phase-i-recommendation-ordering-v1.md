# Approval Decision: Phase-I Recommendation Ordering Remediation

## Approval Scope

Approve one bounded canonical remediation task for the Phase-I no-finalist recommendation ordering defect diagnosed from the exhausted BTCUSDT 4h v2 candidate/geometry trial.

Approved handoff:

```text
plans/architect-to-coder-trendline-family-phase-i-recommendation-ordering-v1.md
```

## Blocking Issues

The existing v2 bundle cannot be trusted through the approved artifact browser until recommendation identity is independent of primary-trial input ordering.

No other blocker is currently established.

## Blast Radius Confirmation

Authorized code scope:

```text
src/libs/models/trendline_family/optimization/evaluator.py
focused optimization/research tests
coder-to-review handoff
codebase-memory index artifacts
```

Explicitly unauthorized:

```text
network requests
real-data Phase-I reruns
v1/v2 artifact rewrites
BinanceNativeAdapter changes
runtime/config/YAML changes
RegimeV2, signal, selection, tracker, interaction, MTF work
```

## Validation Sufficiency

The diagnosis is supported by a read-only independent reconstruction:

```text
winner: None
different recommendation fields:
  parameter_effect_audits
  recommendation_id
same audit set: true
```

The defect is caused by caller-order dependence in the no-finalist recommendation path, not by market data, candidate metrics, holdout evidence, or tampering.

## Residual Risk

The existing v2 bundle may expose a second independent verification defect after ordering is repaired. The coder must stop rather than widen scope if that occurs.

The persisted recommendation is `REJECT` with no finalist, but no metric or model-quality interpretation is approved until full bundle verification passes.

## Approval Decision

**Conditionally approved for canonical ordering remediation and read-only verification of the existing v2 bundle.**

No new Binance request, Phase-I rerun, artifact rewrite, holdout action, or tracker trial is approved.

## Required Handoff

Codex must return:

```text
plans/coder-to-review-trendline-family-phase-i-recommendation-ordering-v1.md
```

The next step after that is independent review. Candidate metrics and follow-on trial planning remain blocked until approval of the verified bundle.
